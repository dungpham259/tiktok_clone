import 'package:flutter/material.dart';

class VideoProfileItem extends StatelessWidget {
  const VideoProfileItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
